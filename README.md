1. Within a Github action that runs whenever code is pushed because it allows tests to happen whenever the code changes, ensuring the code is always tested and working propely after every change. Running tests locally isn't good since you might forget to test your code after each change, and running them after is bad since bugs can build up and be harder to fix later after all layers of the code are implemented and interacting with each other.
2. No
3. Navigation mode analyzes a pages performance metrics right after it loads but can't analyze interactions or changes in content, while snapshot mode analyzes the page in its current state but cannot analyze page performance like in navigation mode.
4. Based on the Lighthouse results, we can improve its accessibility by adding the lang attribute to the html element, SEO by adding a meta description for the document, and network dependency tree by avoiding chaining critical requests through reducing length of chains, download size of resources, or deferring the download of unnecessary resources to improve page load.




